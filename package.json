{
  "name": "mashroom-mcp-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Mashroom MCP Service Monorepo",
  "author": "Milan Heimschild",
  "license": "MIT",
  "repository": {
    "url": "https://github.com/mheimschild/mashroom-mcp"
  },
  "scripts": {
    "build": "pnpm -r build",
    "check": "pnpm -r check",
    "check:fix": "pnpm -r check:fix",
    "format": "pnpm -r format",
    "type-check": "pnpm -r type-check",
    "test": "pnpm -r test"
  },
  "devDependencies": {}
}
